<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing is backed up: the pairing token must not leave the phone. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
